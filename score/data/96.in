(letrec ([fib (lambda (n k)
                (if (or (= n 0) (= n 1))
                    (k 1)
                    (fib (- n 1)
                         (lambda (w)
                           (fib (- n 2) (lambda (v) (k (+ w v))))))))])
  (fib 10 (lambda (x) x)))
